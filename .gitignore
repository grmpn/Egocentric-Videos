# Local Obsidian vault settings and workspace state
/.obsidian/

# External HaWoR checkout contents
/external/HaWoR/

# Immutable local inputs, derived clips, and run evidence (never committed)
# Ignore the directories themselves so dependency-local rules cannot unignore files.
/data/
/outputs/

# Existing local milestone summaries and figures stay private at this legacy path.
# knowledge/wiki/ (including action logs) is intentionally tracked.
/knowledge/agent/summaries/

# Python/Conda build and test caches
__pycache__/
*.py[cod]
.pytest_cache/
.conda/
/environment/condaenv.*.requirements.txt

# Isolated LeRobot dataset/annotation environment
/.venv-lerobot/
